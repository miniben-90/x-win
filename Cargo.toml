[package]
authors      = ["BENKHADRA Hocine <miniben90@hotmail.com>"]
description  = "This package allows you to retrieve precise information about active and open windows on Windows, MacOS, and Linux. You can obtain the position, size, title, and other memory of windows."
edition      = "2021"
keywords     = ["window", "active", "current", "position", "open"]
license      = "MIT"
name         = "x-win-napi"
repository   = "https://github.com/miniben-90/x-win"
rust-version = "1.79.0"
version      = "0.0.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
# Default enable napi4 feature, see https://nodejs.org/api/n-api.html#node-api-version-matrix
base64 = "0.23.0"
napi = { version = "3.12.0", default-features = false, features = [
  "napi4",
  "async",
  "compat-mode",
  "tokio_fs",
  "tokio_time"
] }
napi-derive = "3.6.1"
once_cell = "1.21.4"
x-win = { path = "./x-win-rs" }

[build-dependencies]
napi-build = "2.4.0"

[profile.release]
lto   = true
panic = "abort"
